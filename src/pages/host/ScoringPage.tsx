import Scoring from '../Scoring/index.tsx';

/**
 * Phase 1-5: Wraps the existing Scoring component.
 * Phase 6: Will be migrated to use event data from DB.
 */
export default function ScoringPage() {
  return <Scoring />;
}
