import Ranking from '../Ranking.tsx';

/**
 * Phase 1-5: Wraps the existing Ranking component.
 * Phase 6: Will be migrated to use event data from DB.
 */
export default function RankingsPage() {
  return <Ranking />;
}
